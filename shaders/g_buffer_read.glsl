#ifndef G_BUFFER_READ_H
#define G_BUFFER_READ_H

#include <shader_locations.h>

PredefinedUniform(sampler2D, u_g_buffer_albedo);
PredefinedUniform(sampler2D, u_g_buffer_normal);

#endif // G_BUFFER_READ_H
